sudo: required
language: rust
rust:
  - stable
  - beta
  - nightly
os:
  - linux
matrix:
  allow_failures:
    - rust: nightly
branches:
  only:
    - "master"
install:
- curl https://static.rust-lang.org/rustup.sh |
    sh -s -- --prefix=$HOME/rust
